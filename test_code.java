

while(scan.hasNext()){
    String [] tempArr = scan.next().split(",");

    characters.add(new Character(tempArr[0], // name
                                tempArr[1], // fictional world
                                tempArr[2], // creator
                                tempArr[3], // 
                                tempArr[4]))
}

((tempArr[n].equals("TRUE")))